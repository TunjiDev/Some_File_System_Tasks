{
  "name": "nodejs_fs_practice_2",
  "version": "1.0.0",
  "description": "Checks if a folder exists, if it doesn't creates the folder, then fetches data from a jsonplacedholder post, writes the data to the created folder and reads it on the server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Tunji",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
